## 2023-10-27 - Keyboard Navigation for Interactive Cards\n**Learning:** Interactive 'cards' implemented as `motion.div` with `cursor-pointer` require explicit keyboard support (`tabIndex`, keyboard event handlers, and focus styles) for proper accessibility.\n**Action:** Add `tabIndex={0}`, `focus-visible` classes, and focus event handlers to make custom interactive elements keyboard-accessible.
