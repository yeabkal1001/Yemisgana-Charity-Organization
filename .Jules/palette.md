## 2024-05-18 - Improve social icon accessibility
**Learning:** For icon-only interactive elements mapping over components, explicitly mapping label strings within the objects array provides direct variables for screen readers and allows straightforward integration of keyboard-accessible focus-visible utilities on the outer container.
**Action:** Always include label strings directly in the mapped array data to assign ARIA attributes and focus styles consistently for screen reader users and keyboard navigation.
