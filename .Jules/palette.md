## 2024-08-05 - Global Focus-Visible Styling
**Learning:** Adding a global `:focus-visible` rule in `globals.css` using design tokens is more robust for keyboard accessibility across the design system than manually applying Tailwind `focus-visible:` utilities to individual components.
**Action:** Use this pattern to establish baseline keyboard navigation support system-wide, while ensuring mouse/touch interactions don't trigger unnecessary focus rings.
