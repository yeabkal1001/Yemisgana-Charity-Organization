## 2024-05-30 - Added Global Focus Visible Rules\n**Learning:** The application lacked any keyboard accessibility focus rings, making interactive elements invisible to keyboard users.\n**Action:** Add a global `:focus-visible` rule in `app/globals.css` as an exception to the 'use existing classes' constraint, to automatically give all interactive elements a standardized focus ring that uses design tokens (like `var(--color-lime)`). This is the approved method for global keyboard accessibility.
